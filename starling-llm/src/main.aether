module Main;

import Config;
import Gateway;

func main() -> Int {
    let config = Config.create_config(
        8080,
        "models/llama2-7b.gguf",
        32
    );

    // Start gateway logic
    
    return 0;
}
