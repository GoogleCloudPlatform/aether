module TokenizerImpl;

import Types;
import Tokenizer;
import Json;
import Io;

pub struct BPETokenizer {
    model_path: String,
}

impl Tokenizer.Tokenizer for BPETokenizer {
    func encode(self: &Self, text: String) -> Array<Types.TokenId> {
        let result: Array<Types.TokenId> = []; 
        return result;
    }

    func decode(self: &Self, tokens: Array<Types.TokenId>) -> String {
        return "";
    }

    func vocab_size(self: &Self) -> Int {
        return 32000;
    }

    func eos_token_id(self: &Self) -> Types.TokenId {
        return 2;
    }
}

pub func load_from_file(path: String) -> BPETokenizer {
    return BPETokenizer {
        model_path: path,
    };
}
