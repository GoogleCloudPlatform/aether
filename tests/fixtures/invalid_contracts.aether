module invalid_contracts {
  // Intent: Test module with invalid contracts

  @intent("Function with invalid performance expectation")
  @perf(metric="LatencyMs", target=-10.0, context="Invalid negative latency")
  @complexity(type="Time", notation="BigO", value="O(invalid)")
  func bad_performance(x: Int) -> Int {
    return x;
  }
}
